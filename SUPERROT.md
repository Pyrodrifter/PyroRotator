# SuperRot protocol

The canonical SuperRot v1 specification is maintained with the SkyPhreak host:

[`../docs/SUPERROT_PROTOCOL.md`](../docs/SUPERROT_PROTOCOL.md)

Keeping a single specification prevents the firmware and host from drifting apart.
